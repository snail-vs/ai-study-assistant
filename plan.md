# 学习验证引擎方案与接入计划

## 目标

在保留现有“理解检查”闭环的基础上，将 Quiz 专用实现演进为可组合的学习验证引擎。引擎应支持“识别 → 回忆/解释 → 应用 → 迁移/反思”的验证层级，并能按学习目标组装题型、收集不同形式的作答证据、选择确定性或 AI 评价器，再针对具体薄弱点进行一次有边界的追问或补救。

本轮只完成设计文档和接入评估，不修改业务代码、数据库或 API。

## 已有能力基线

- `LearningActivity` 已挂载在章节上，支持 `quiz | practice | lab` 扩展位。
- 现有 Quiz 支持 `single_choice | true_false | short_answer`。
- 题面与答案键分开持久化，查询接口不会返回答案键。
- 客观题由后端确定性判分，简答题由 `AssessmentAgent` 按 rubric 评价。
- `ActivityAttempt` 已保存答案、逐题结果、总分、掌握级别和诊断摘要。
- 活动结果已能触发导师反馈与前置知识诊断。
- 前端目前是集中在 `App.vue` 内的 Quiz 专用渲染和提交流程。

## 核心设计原则

1. 不按学科硬编码引擎，而按“目标能力、证据形式、评价方式、反馈动作”抽象。
2. AI 负责受约束的内容生成、语义评价和诊断；确定性规则、状态机、安全门槛由系统负责。
3. 题型展示组件与评价器解耦；同一种证据可以由不同评价器组合处理。
4. 现有 Quiz 作为 `specVersion: 1` 或兼容适配层继续工作，避免一次性迁移。
5. 追问针对缺失证据和首个认知断点，默认最多一次；不会演变成无边界聊天。
6. 实验、代码、绘画等高不确定性活动采用分维度 rubric，不把 AI 单一总分当作唯一事实。

## 需要产出的正式方案文档

建议新增 `docs/learning-validation-engine.md`，至少覆盖：

- 产品定位与现有“理解检查”的关系；
- 统一领域模型（目标、任务、证据、评价、诊断、干预）；
- `AssessmentSpec`、`TaskSpec`、`EvidenceSpec`、`EvaluationSpec`、`FollowUpSpec` 的建议结构；
- 题型/回答组件注册机制；
- 规则评价、测试执行、AI rubric、视觉评价等评价器边界；
- 尝试状态机与一次追问流程；
- 理论、数学、编程、绘画、实验课程的组装示例；
- 与现有模型、接口、Agent、前端的逐点接入方案；
- 向后兼容、数据迁移、可观测性、安全和成本控制；
- 分阶段交付与验收标准。

## 推荐接入路线

### Phase 0：规范落地

- 完成设计文档并确认术语、边界和最小闭环。
- 不引入新的学科枚举，以能力和证据维度驱动组装。

### Phase 1：兼容式内核

- 在现有 `LearningActivity.content_json` 中引入版本化 `assessmentSpec`。
- 使用适配器把旧 `questions`/`answer_key` 映射到统一任务和评价规格。
- 后端拆出生成、作答校验、评价编排、诊断汇总服务；保持当前接口可用。
- 第一阶段只注册现有三种题型和两类评价器：确定性评价、AI rubric 评价。

### Phase 2：解释与针对性追问

- 评价结果增加命中/缺失 rubric、错误类型、置信度和 `followUpDecision`。
- 增加一次追问任务及其父子关系，保存追问答案与前后变化。
- 前端从一次性结果页改成“答题 → 反馈/追问 → 完成”的小型状态机。

### Phase 3：应用与结构化过程证据

- 增加排序、步骤、结构化过程题。
- 支持数学中间步骤和实验记录等 JSON 证据。
- 评价器可组合规则校验与 AI 诊断。

### Phase 4：代码和作品证据

- 增加代码编辑器、图片/文件上传等回答组件。
- 代码优先使用受控测试执行结果；AI 负责解释失败原因。
- 绘画和作品使用可见 rubric、分维度反馈和修改前后对比。
- 实体实验加入安全约束、过程检查点及必要的人工确认。

## 预计受影响区域（编码阶段）

- `backend/agents/schemas.py`：规格、任务、评价及追问结构。
- `backend/agents/assessment_agent.py`、`backend/agents/prompts.py`：规格生成、rubric 评价和追问生成。
- `backend/api.py`：逐步瘦身为端点编排，并调用独立领域服务。
- `backend/models.py` 与迁移：仅在 JSON 无法满足查询/追踪需求时新增任务实例或追问关系表。
- `backend/schemas.py` 与 `contracts/openapi.yaml`：版本化活动、尝试和结果契约。
- `frontend/src/App.vue`：先抽离学习活动容器，再按题型建立组件注册表。
- 新增后端 assessment/application service 与前端 activity components（具体路径在实现前确认）。

## 关键决策点

- 首版是否只做“解释 + 一次追问”，暂不进入上传型证据。
- 新规范是继续保存在 `content_json`，还是拆成可查询的任务实体；建议先 JSON、后实体化。
- 活动是预生成整套路径，还是首题生成后动态派生；建议基础任务预生成、追问动态生成。
- 掌握度是否继续只用总分；建议保留兼容总分，同时新增按目标/rubric 的掌握证据。
- AI 低置信度时如何处理；建议反馈降级为“待确认/建议复习”，不自动触发强结论。

## 文档验收标准

- 能解释为什么现有选择题与新解释/追问不重复。
- 能用同一模型描述概念、数学、编程、绘画和实验活动。
- 明确现有代码各层如何渐进接入，且 Phase 1 不破坏当前 Quiz。
- 数据结构足以描述题面、证据、评价结果、诊断和后续动作。
- 明确 AI 与确定性系统各自负责什么，并包含失败降级策略。
- 给出可独立开发和验证的阶段、测试范围与验收路径。
