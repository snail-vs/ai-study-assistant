apiVersion: v1
kind: Service
metadata:
  name: studycenter-backend
  namespace: studycenter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: studycenter-backend
  ports:
    - name: http
      port: 8000
      targetPort: http

