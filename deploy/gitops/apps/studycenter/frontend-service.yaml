apiVersion: v1
kind: Service
metadata:
  name: studycenter-frontend
  namespace: studycenter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: studycenter-frontend
  ports:
    - name: http
      port: 80
      targetPort: http

