# 后续迭代清单

## AI 任务与跨页签恢复

### 第一阶段：轻量任务状态（当前实现）

- [x] 为旁支 AI 请求持久化 Run 状态。
- [x] 防止同一旁支会话重复提交未完成的问题。
- [x] 页面刷新或新页签打开时查询未完成 Run。
- [x] 记录 AI 任务阶段和失败原因。
- [ ] 前端根据未完成 Run 恢复提示并轮询最终状态。

### 第二阶段：完整跨页签和断线恢复

- [ ] 将课程生成和旁支问答统一为后台异步任务。
- [ ] 保存增量输出内容。
- [ ] SSE 支持 `Last-Event-ID` 事件回放和断线重连。
- [ ] 多页签实时同步同一个 Run 的状态和输出。
- [ ] 增加任务取消、超时、重试和并发策略。
